#include <stdlib.h>
#include <time.h>
#include <stdio.t>
/* more headers goes there */

/* betty style doc for function main goes there */
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	for(n=0;n;n<=26;c++);
	{
	     putchar(n);
        }
	printf(\n);
	return (0);
}
