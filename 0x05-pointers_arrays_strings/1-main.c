#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main (void)
{
	int x;
	int y;

	x = 98;
	b = 42;
	printf("x=%d,b=%d\n", x, y);
	swap_int(&x, &y);
	printf("a=%d, b=%d\n",x, y);
	return (0);
}
